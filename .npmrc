# Don't re-verify the dependency graph before every `pnpm run` (pnpm 11 default
# is on, and it fails the run when an optional build script — e.g. esbuild's,
# pulled in by vitest — is flagged "ignored"). The esbuild platform binary is
# prebuilt, so scripts run fine without it.
verify-deps-before-run=false
